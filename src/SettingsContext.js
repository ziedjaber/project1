import React from "react";

const defaultValue = { /* votre valeur par défaut ici */ };
const SettingsContext = React.createContext(defaultValue); // Utilisation de defaultValue comme valeur par défaut
export default SettingsContext;
